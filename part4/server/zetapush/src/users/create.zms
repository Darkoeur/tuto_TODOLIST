macroscript create (string login, string password) {
	
	var user = auth.createUser({
		'login': login,
		'password': password
	});
	
	// Auto registration to the global GroupStack
	call addMeToGroupStack({groupId: GROUP_DEFAULT_ID});
	
} return {user} on channel __selfName